# my-pro-resume

A single-page resume built with React 19, TypeScript, Vite, Tailwind CSS, and Framer Motion.

## Commands

```bash
npm install
npm run dev      # development
npm run build    # production build (output: dist/)
npm run preview  # preview production build
npm run lint     # lint
```
